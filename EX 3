const QUESTIONS = [
    ["Quelle est la capitale de la France ?", "Paris"],
    ["Quel est le club de foot de Barcelone ?", "FCB"],
    ["Est-ce que JavaScript est un langage ?", "Oui"]
];
function lancerQuiz() {
    let score = 0; // compteur de réponses correctes
    for (let i = 0; i < QUESTIONS.length; i++) {
        let question = QUESTIONS[i][0];
        let reponseCorrecte = QUESTIONS[i][1];
        let reponseUtilisateur = prompt(question);
        if (reponseUtilisateur === reponseCorrecte) {
            alert("Réponse juste");
            score++;
        } else {
            alert("Réponse fausse");
        }
    }
    alert("Vous avez répondu correctement à " + score + " questions sur " + QUESTIONS.length);
}
